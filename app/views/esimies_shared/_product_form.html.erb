
<%= form_for(@product) do |f| %>
<%= render 'shared/error_messages', object: f.object %>		
		<%= f.label :kuvaus %>
		<%= f.text_field :kuvaus %> <br />
		
		<%= f.label :hinta %>
		<%= f.text_field :hinta %>	<br />
		
		<%= f.label :provisio %>
		<%= f.text_field :provisio %><br />
		
		<%= f.label :tuoteryhmä %>
		<%= select("product", "category_id", Category.all.collect {|c| [ c.kuvaus, c.id ] })%> <br />
		
		<%= f.submit "Luo tuote", class: "btn btn-inverse" %>
<% end %>